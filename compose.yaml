services:
  aries:
    build:
      context: .
      dockerfile: Containerfile
    image: localhost/aries-resume-ats:latest
    ports:
      - "8080:8080"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
